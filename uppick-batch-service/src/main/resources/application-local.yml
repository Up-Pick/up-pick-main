spring:
  datasource:
    url: jdbc:mysql://localhost:3306/uppick?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: password

logging:
  level:
    org.oneog.uppick: DEBUG
    org.springframework.batch: DEBUG
