[mysqld]
# Slave 설정
server-id=2
relay-log=mysql-relay-bin
log-bin=mysql-bin
binlog-format=ROW
read_only=1

# Replication 설정
replicate-do-db=uppick_db

# 문자 인코딩
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
