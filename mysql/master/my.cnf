[mysqld]
# Master 설정
server-id=1
log-bin=mysql-bin
binlog-format=ROW
binlog-do-db=uppick_db

# 성능 최적화
max_binlog_size=100M
expire_logs_days=7
sync_binlog=1

# 문자 인코딩
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci
